// Production config (used by `ng build`).
// ⬇️ Set this to your deployed backend URL (e.g. https://your-app.back4app.io)
export const environment = {
  production: true,
  apiUrl: 'https://YOUR-BACKEND-URL',
};
